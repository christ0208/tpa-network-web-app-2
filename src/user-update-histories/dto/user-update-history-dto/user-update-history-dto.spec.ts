import { UserUpdateHistoryDto } from './user-update-history-dto';

describe('UserUpdateHistoryDto', () => {
  it('should be defined', () => {
    expect(new UserUpdateHistoryDto()).toBeDefined();
  });
});
